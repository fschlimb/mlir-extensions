include_directories(${MLIR_INCLUDE_DIRS})
set(dialect DistOps)
set(dialect_namespace dist)
set(LLVM_TARGET_DEFINITIONS ${dialect}.td)
mlir_tablegen(${dialect}Types.h.inc -gen-typedef-decls -I ${PROJECT_SOURCE_DIR}/include)
mlir_tablegen(${dialect}Types.cpp.inc -gen-typedef-defs -I ${PROJECT_SOURCE_DIR}/include)
mlir_tablegen(${dialect}.h.inc -gen-op-decls -I ${PROJECT_SOURCE_DIR}/include)
mlir_tablegen(${dialect}.cpp.inc -gen-op-defs -I ${PROJECT_SOURCE_DIR}/include)
mlir_tablegen(${dialect}Dialect.h.inc -gen-dialect-decls -dialect=${dialect_namespace} -I ${PROJECT_SOURCE_DIR}/include)
mlir_tablegen(${dialect}Dialect.cpp.inc -gen-dialect-defs -dialect=${dialect_namespace} -I ${PROJECT_SOURCE_DIR}/include)
add_public_tablegen_target(MLIR${dialect}IncGen)
add_dependencies(mlir-headers MLIR${dialect}IncGen)
